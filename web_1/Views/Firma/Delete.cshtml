@model EfCore2C.Models.Firma
﻿@using System.Globalization
@using Microsoft.AspNetCore.Mvc.Localization

@inject IViewLocalizer localizer
@{
    Layout = "_AdminLayout";
}


<h2>Delete Confirmation</h2>
<p>Are you sure you want to delete this Firma?</p>

<div>
    <h4>Firma</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.firma_adi)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.firma_adi)
        </dd>
    </dl>

    <!-- Add other properties to display here -->

    <form asp-action="Delete" asp-route-id="@Model.firma_id" method="post">
        @Html.AntiForgeryToken()

        <div class="form-group">
            <input type="submit" value="@localizer["Delete"]" class="btn btn-danger" />
            <a asp-action="List">Back to List</a>
        </div>
    </form>
</div>
